# 扩扩帮｜新用户头像授权页交互说明

## 1. 原型定位

- **原型类型**：低保真 / 中保真移动端产品原型
- **画布尺寸**：375 × 812 px
- **页面目标**：让新用户理解头像用途、感知授权价值，并在低压力环境下完成授权或跳过。
- **页面主题**：让你的头像，成为更清晰的协作名片。
- **信息顺序**：顶部导航 → 头像预览 → 主题与副说明 → 3项授权价值 → 授权说明 → 主按钮 → 次操作。

## 2. 组件清单

| 组件名 | 页面职责 | 关键内容 |
|---|---|---|
| `TopNav` | 返回与当前页面定位 | 返回箭头、设置头像 |
| `AvatarPreview` | 展示授权前后头像 | 动态变量 `{avatar}`、默认头像、头像状态 |
| `IntroSection` | 说明授权价值主张 | 让你的头像，成为更清晰的协作名片 |
| `AvatarValueCard` | 降低授权疑虑 | 更容易被认识、协作更清晰、体验更完整 |
| `PermissionNote` | 明确头像使用范围 | 头像用于扩扩帮内的身份展示 |
| `AuthorizeButton` | 唯一主行动入口 | 授权头像 |
| `SkipLink` | 提供低压力替代路径 | 暂不设置 |
| `SkipConfirmDialog` | 防止误触跳过 | 留下设置、暂不设置 |
| `FeedbackToast` | 提供即时操作反馈 | 头像已更新、已跳过头像设置等 |

## 3. 默认状态

- 顶部导航显示“设置头像”。
- `AvatarPreview` 使用灰色默认头像占位，数据占位符为 `{avatar}`。
- 头像状态显示“默认头像”。
- 主按钮显示“授权头像”，使用蓝色满宽按钮。
- 次操作“暂不设置”使用普通文字链接，视觉权重低于主按钮。
- 不使用红色警告、倒计时或强制授权文案。

## 4. 授权流程

1. 用户点击 `AuthorizeButton`「授权头像」。
2. 原型打开系统文件选择器，模拟微信头像授权流程。
3. 用户选择图片后：
   - 将图片替换默认头像；
   - 更新 `AvatarPreview` 的 `{avatar}`；
   - 状态显示“头像已更新”；
   - 页面顶部原型状态更新为“授权成功 · 已展示真实头像”；
   - 显示轻量反馈“头像已更新”。
4. 真实小程序接入时，应将文件选择器替换为微信头像授权接口，并在返回页面后更新头像。

## 5. 授权拒绝与加载失败

### 用户拒绝授权

- 保留默认头像。
- 不出现责备、阻断或红色错误提示。
- 可以显示轻量提示：“暂未获取头像，仍可继续使用”。
- 用户仍可以再次点击「授权头像」重新尝试。

### 头像加载失败

- 删除失败图片并恢复默认头像。
- 显示：“头像加载失败，已保留默认头像”。
- 不影响用户继续使用或点击「暂不设置」。

## 6. 跳过流程

1. 用户点击 `SkipLink`「暂不设置」。
2. 居中出现 `SkipConfirmDialog`：
   - 标题：暂不设置头像？
   - 说明：你可以先进入扩扩帮，之后再回来设置。
   - 次按钮：留下设置
   - 主按钮：暂不设置
3. 点击「留下设置」或右上角关闭按钮：关闭弹窗，回到当前页面。
4. 点击「暂不设置」：
   - 关闭弹窗；
   - 显示“已跳过头像设置，继续进入扩扩帮”；
   - 真实小程序中调用后续流程 / 首页跳转。

## 7. 返回操作

- 点击左上角返回箭头，原型中显示“返回上一页”。
- 接入微信小程序后对应 `wx.navigateBack()`。
- 返回不改变用户当前头像数据；如果已成功授权，应由上一级页面读取已更新的头像。

## 8. 文案原则

- 用“头像会用于哪里”解释用途，而不是强调必须授权。
- 价值从用户体验出发：被认识、协作确认、身份统一。
- 保留跳过路径，降低心理压力。
- 授权成功给正向反馈；拒绝和失败保持中性，不对用户施加压力。
